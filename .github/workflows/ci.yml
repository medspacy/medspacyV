name: CI

on:
  push:
    branches: [main, "feature/**"]
  pull_request:
    branches: [main]

jobs:
  lint:
    name: Lint (ruff)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.10"
      - name: Install ruff
        run: pip install ruff
      - name: Run ruff
        run: ruff check .

  test:
    name: Tests (Python ${{ matrix.python-version }})
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.8", "3.12"]

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          # PyRuSH 1.0.13 has a setuptools 75.x incompatibility in its pyproject.toml.
          # Pre-install it with --no-build-isolation using setuptools<75 to work around this.
          pip install cython "setuptools<75"
          pip install pyrush --no-build-isolation
          pip install -r requirements-dev.txt
          python -m spacy download en_core_web_sm || true

      - name: Run unit tests
        run: pytest -m unit --cov=. --cov-report=term-missing

      - name: Run integration tests
        run: pytest -m integration
